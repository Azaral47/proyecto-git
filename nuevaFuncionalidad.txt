# esta es una función básica de suma
def suma(a, b):
  return a + b

result = suma(1, 2)
# result = 3

def duplica(num):
    x = num * 2
    return x

print(x)  # error - x no está definida
print(duplica(4))  # muestra 8